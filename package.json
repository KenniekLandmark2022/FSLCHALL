{
  "devDependencies": {
    "eslint": "^8.31.0",
    "jest": "^29.3.1",
    "prettier": "^2.8.2"
  }


  "scripts": {
    "lint": "eslint . --ext .js",
    "prettier": "prettier --write \"**/*.{js,json,css,md}\"",
    "test": "jest",
    "build": "npm run build:prod"
  },

}